# Prompt history — single-page-loop-02

`human_use.applicability = applicable`. Rationale: The Artifact is a human-operated mobile checkout. Its success depends on discoverability, navigation, input burden, error recovery, feedback, accessibility, touch ergonomics, interruption recovery, perceived latency, destructive-action safety, and cognitive load across cross-disability use.

## Complete Track instruction acted on

Execute exactly one improvement Loop as approved model role `generator-single-page`. Create `single-page-loop-02` from immutable parent `single-page-loop-01` with `parent_ids: ["single-page-loop-01"]`. Own only `.experiments\accessible-mobile-checkout\generated\track-single-page\**`; never modify Loop 01, its failed evidence, the approved setup, frozen harness or fixture, another Track, top-level assembly, Viewer, Navigation Evidence, Evidence Gate, judging, promotion, repository source, dependencies, or git history.

Remain a single-page landmarked form with every required checkout section present, direct editability, and a persistent order summary. Do not become a wizard or independent task-card flow. Copy the parent candidate files, then apply the accepted independent feedback: reduce traversal and the 13-item error-state burden through compact completed-section summaries and optional progressive disclosure; restore appropriate autocomplete tokens; replace the shipping double-radio ARIA pattern with native radio inputs styled as cards and native state updates; and accept shape-valid synthetic inputs while retaining clear canonical fixture hints and every frozen content, task, privacy, safety, offline, mobile, accessibility, and duplicate-placement gate.

Use only local original HTML, CSS, and JavaScript. Keep the canonical fixture visible and ensure the frozen harness still enters every exact canonical value. Restore only safe contact, delivery, and shipping progress; never persist payment details. Keep placement explicit, local, network-free, deterministic, duplicate-safe, and limited to exactly one `SYN-2048` confirmation.

Qualitative use-friction is not an objective ergonomics gate. Objective browser assertions cover only frozen technical correctness and compatibility requirements. Independent judges must later assess exactly: `discoverability`, `navigation`, `input_burden`, `error_prevention_recovery`, `feedback_status`, `accessibility`, `responsive_touch_ergonomics`, `interruption_resumption`, `latency_perception`, `destructive_actions`, and `cognitive_load`. Severe qualitative friction must remain visible in scores, findings, caveats, and dissent, but cannot override or masquerade as an objective result. Failed or degraded digital operations are defects and cannot be hidden by visual polish.

Run the unchanged frozen objective command. Preserve every failed evidence attempt separately, repair only Loop 02, and stop failed unless all seven objective gates pass. Record the exact feedback, objective evidence, non-gating metric comparison to Loop 01, hashes, one durable lesson, terminal heartbeat, and decision `needs_human_review`. Do not self-judge or claim `new_best`.

## Input feedback

Accepted instruction (authoritative):

Within the single-page landmarked paradigm, reduce traversal and error-state load with compact completed-section summaries or optional progressive disclosure that keeps landmarks and direct editability; restore appropriate autocomplete tokens, use native radio inputs styled as cards without duplicate ARIA roles, and relax exact-fixture validation to shape-valid synthetic inputs while retaining clear fixture hints and all frozen invariants.

Feedback sidecar: `.experiments/accessible-mobile-checkout/generated/judging/loop-01/aggregate/loop-02-feedback.json`, exact entry `single-page-loop-01`.

### Evidence 1

Source: `judging/loop-01/judge-accessibility/candidate-b.json`

Attribution: `judge-accessibility defect`

```text
At 390px, the complete one-page form and 13-item error state are cognitively and physically long; the user must manage many fields before review.
```

### Evidence 2

Source: `judging/loop-01/judge-human-use/candidate-b.json`

Attribution: `judge-human-use input burden finding`

```text
Validation is strict-fixture: state must equal 'NSW' (case-sensitive), country must equal 'australia' (lowercased); card number must equal '4111111111111111'; expiry must equal '12/34'; CSC must equal '123'; cardholder must equal 'alex morgan'. Any real-world variation is rejected. All inputs also have autocomplete='off', which disables browser autofill / password-manager assistance — a documented accessibility anti-pattern for motor-constrained and cognitively-impaired users. The 'Synthetic value:' helper text and fixture-card do mitigate for sighted users who read them, but the underlying interaction burden is high. Evidence: candidate-b/app.js fieldRules + errorMessages, candidate-b/index.html autocomplete='off' on every input.
```

### Evidence 3

Source: `judging/loop-01/judge-human-use/candidate-b.json`

Attribution: `judge-human-use accessibility finding`

```text
Solid landmark tree (header/nav/main/aside/footer), fieldset+legend per section, aria-describedby chained, error summary role='alert' with tabindex=-1 that receives focus after invalid submit. Concerns: (a) Shipping uses a double-radio anti-pattern — <label class='choice-card' role='radio' aria-checked> wrapping a real <input type='radio' aria-hidden='true'> — screen-reader users may hear the group twice or miss state updates because aria-checked is set only via the keydown handler, not by the input's own change; (b) autocomplete='off' on every field is an anti-pattern for AT-assisted form filling; (c) no visible skip-link even though the section-nav helps sighted keyboard users. Evidence: candidate-b/index.html lines 171–184; candidate-b/app.js keydown handler on shipping-* elements.
```

### Evidence 4

Source: `judging/loop-01/judge-human-use/candidate-b.json`

Attribution: `judge-human-use cognitive load finding`

```text
All five sections are on one continuously scrolled page. Nav pills help orientation, but ~20 required fields plus fixture cards plus a review block are visible at once, which is heavy on a small viewport. The exact-match validation adds hidden rules ('why won't it accept 4111-1111-1111-1111 with dashes?') that a real cross-disability user must reason through. The review block is helpful but reprints only aggregate values rather than every field verbatim, so users must scroll back to correct.
```

Mapped raw judge files read:

- `.experiments/accessible-mobile-checkout/generated/judging/loop-01/judge-accessibility/candidate-b.json`
- `.experiments/accessible-mobile-checkout/generated/judging/loop-01/judge-human-use/candidate-b.json`

No unrelated candidate feedback was read or consumed.

## Provisional self-observation — non-authoritative

The unchanged frozen harness passed on preserved attempt 02 after attempt 01 failed only because a Loop 02 JavaScript scoping defect prevented the grouped error summary from rendering. Attempt 01 remains under `evidence-attempt-01-failed`; the repair was limited to Loop 02. All seven frozen gates pass in `evidence`. Supplemental deterministic checks confirm the requested autocomplete tokens, two native shipping radios with native arrow-key state updates, a four-section summary for the 13-field empty error state, successful completion with noncanonical shape-valid synthetic values, and four optional compact completed-section summaries with direct edit and focus restoration. These checks are supplemental and do not alter the frozen objective gate.

Loop 02 non-gating metrics are 25 focusables, 20 activations, 4 corrections, and 1 completion, compared with Loop 01 at 25, 13, 4, and 1: deltas 0, +7, 0, and 0. The counts do not establish qualitative improvement and do not override the independent feedback.

## Proposed next Prompt

Have the approved independent judges operate `single-page-loop-02` against the same evidence bundle and assess every frozen qualitative lens, including whether optional compact summaries genuinely reduce traversal without obscuring direct editing and whether grouped errors remain sufficiently actionable. Preserve dissent and compare against `single-page-loop-01`. Keep Loop 02 at `needs_human_review` unless independent evidence supports a later orchestrator decision; do not let the generator judge or promote its own work.
